# 金融长文档智能阅读理解系统架构设计文档（MVP版）

## 1. 文档说明

### 1.1 文档目的

本文档面向竞赛首版原型开发阶段，给出“金融长文档智能阅读理解系统”的 MVP 版本系统架构设计方案。该版本不追求一次性覆盖全部高级能力，而是围绕赛题硬约束，优先构建一套 `合规、可运行、可追溯、可提交` 的最小可行系统。

### 1.2 赛题约束下的建设目标

结合工作区内两份赛题分析文档，MVP 版本聚焦以下目标：

- 跑通 `离线预处理 -> 在线检索 -> Qwen 证据筛选 -> Qwen 逐选项推理 -> 答案输出` 的核心链路
- 满足赛题对正式答题阶段的合规要求，避免使用不允许的非 Qwen 语义能力
- 支持 A 榜和 B 榜两种运行模式
- 输出可追溯的 `answer.csv` 与 `evidence.json`
- 在保证准确率的前提下控制 Token 消耗

### 1.3 适用范围

本版本适用于以下场景：

- 竞赛首版原型搭建
- A 榜样例联调与策略验证
- B 榜前的文档级召回基线构建
- 金融产品、监管法规、金融合同、财报、研报等长文档的基础阅读理解

## 2. 设计原则

- `合规优先`：正式答题链路中，推理、证据筛选、答案判断统一收口到 Qwen。
- `离线重、在线轻`：复杂 PDF 结构恢复放在离线阶段完成，在线尽量只处理少量高价值证据。
- `词法打底`：MVP 阶段检索主力采用 BM25、倒排、条款号匹配、数字匹配等纯规则方法。
- `逐选项推理`：不直接让模型吐最终字母，先对每个选项给出 `support/refute/insufficient`。
- `证据可追溯`：所有结论必须附着文档来源、页码、条款号或原文引用。
- `少读少重复读`：通过候选收缩、补检索和记忆压缩降低无效 Token。

## 3. MVP 版本目标能力

MVP 版本建议实现以下核心能力：

- 支持 PDF、扫描版 PDF、TXT 文档导入
- 支持 OCR、版面恢复、基础表格提取和结构化切分
- 支持 A 榜 `已知 doc_ids` 直达模式
- 支持 B 榜 `未知 doc_ids` 的文档级词法召回
- 支持题干级和选项级片段检索
- 支持 Qwen 对候选证据做精筛
- 支持 Qwen 对 A/B/C/D 逐选项判断
- 支持数值比较和基础公式计算
- 支持结构化记忆压缩与 Token 统计
- 支持输出 `answer.csv`、`evidence.json` 和运行日志

MVP 版本暂不作为重点的内容：

- 基于向量数据库的语义检索主链路
- 非 Qwen 模型的 rerank、embedding、语义过滤
- 知识图谱和复杂长期记忆系统
- 多智能体并行协作
- 企业级高可用和权限治理

## 4. 系统总体架构

### 4.1 分层架构

MVP 版本采用四层架构：

1. `离线预处理层`：负责 OCR、版面分析、表格恢复、结构化切片与索引构建。
2. `规则召回层`：负责文档级和片段级词法检索、规则匹配和候选收缩。
3. `Qwen 推理层`：负责证据筛选、逐选项判断、必要时的补检索决策。
4. `输出与压缩层`：负责答案格式化、证据链落盘、记忆压缩和 Token 统计。

### 4.2 总体流程

```text
原始 PDF / 文本文档
        |
        v
离线预处理层（OCR、结构恢复、表格抽取、切片、索引）
        |
        v
规则召回层（A榜 doc_ids 直达 / B榜文档召回 / chunk召回）
        |
        v
Qwen 推理层（证据筛选、逐选项判断、必要时补检索）
        |
        v
输出与压缩层（answer.csv、evidence.json、结构化记忆、Token日志）
```

### 4.3 MVP 逻辑架构图

```text
前端页面 / 命令行 / 批处理脚本
        |
        v
任务编排器
        |
        +-----------------------------+
        |                             |
        v                             v
离线预处理模块                  在线答题模块
        |                             |
        v                             v
结构化文档仓库                 规则召回 + Qwen精筛 + Qwen推理
        |                             |
        +---------------+-------------+
                        |
                        v
               证据链与答案生成模块
                        |
                        v
               记忆压缩与Token统计模块
```

## 5. 赛题适配设计

### 5.1 A 榜与 B 榜双模式

MVP 版本必须天然支持两种答题模式：

- `A榜模式`：题目提供 `doc_ids`，系统直接在指定文档范围内做片段检索和推理。
- `B榜模式`：题目不提供 `doc_ids`，系统需先做文档级候选召回，再进入片段检索。

因此 MVP 不能只做“文档内问答”，而必须具备 `doc-level -> chunk-level` 的两级检索能力。

### 5.2 合规边界

根据赛题分析，MVP 版本需明确以下边界：

- 允许在离线预处理阶段使用 OCR、版面分析、PDF 转结构化等非 Qwen 工具。
- 正式答题阶段不依赖非 Qwen 模型生成的向量、排序结果和语义结论。
- MVP 正式检索主链路使用纯词法/规则方法，避免合规争议。
- Qwen 统一承担证据精筛、逐选项推理和必要的压缩任务。

### 5.3 评分导向

MVP 版本优先策略为：

- 第一优先：提升准确率
- 第二优先：保证答案格式零失误
- 第三优先：在不掉准确率的前提下降低 Token

因此 MVP 不追求“最低 Token”，而追求“单位 Token 下的稳定正确率”。

## 6. 核心模块设计

### 6.1 离线预处理模块

#### 功能目标

- 将 PDF、扫描版 PDF、TXT 统一转为结构化文本单元
- 恢复页码、章节、条款、表格等结构信息
- 清洗页眉页脚、水印、重复段和目录噪声
- 为后续检索和证据回指提供稳定锚点

#### 处理流程

1. 文档接入
2. PDF 解析或 OCR 识别
3. 版面恢复与阅读顺序还原
4. 表格和条款结构抽取
5. 噪声清洗
6. 结构化切片
7. 建立词法索引与元数据索引

#### 推荐实现

- PDF 解析：`PyMuPDF`
- OCR：`PaddleOCR`
- 表格与结构恢复：`pdfplumber` 或等效工具
- 文本清洗：`Python + Regex + 规则引擎`

#### 标准 chunk 字段建议

| 字段 | 说明 |
|---|---|
| `doc_id` | 文档唯一标识 |
| `title` | 文档标题 |
| `domain` | 文档领域，如 `insurance`、`regulatory` |
| `page_no` | 页码 |
| `section_path` | 章节路径 |
| `chunk_id` | 切片编号 |
| `chunk_text` | 当前片段文本 |
| `chunk_type` | `prose/table/clause/header/footer/appendix` |
| `clause_no` | 条款编号 |
| `table_title` | 表格标题 |
| `entities` | 公司、产品、法规等规则抽取实体 |
| `numbers` | 金额、比例、期限等数值字段 |
| `source_span` | 原文位置范围 |

#### 切片策略

MVP 不建议只按固定字数切片，而采用 `结构优先、长度兜底`：

- 监管法规按 `条 / 款 / 项` 切
- 财报按 `章节 + 表格独立块` 切
- 保险条款按 `责任 / 免责 / 给付 / 退保 / 现金价值` 切
- 研报按 `观点块 / 风险提示块` 切
- 超长块再做二级切分并保留 overlap

### 6.2 规则召回模块

#### 功能目标

- 在不依赖非 Qwen 语义模型的前提下完成高召回
- 支持文档级与片段级两段式检索
- 为后续 Qwen 证据筛选缩小输入范围

#### 检索策略

MVP 阶段采用 `词法 + 规则` 混合召回：

- BM25
- 倒排索引
- 标题词匹配
- 条款编号匹配
- 数字词匹配
- 实体重合匹配
- 领域术语词典扩展

#### 两级检索机制

`doc-level`：

- B 榜模式先将候选文档从几十篇缩小到 3~8 篇
- A 榜模式直接使用题目给定 `doc_ids`

`chunk-level`：

- 不只检索题干
- 同时对题干和每个选项分别检索
- 召回后做去重、合并和排序

#### 为什么做选项级检索

赛题中的关键差异常常在选项细节中体现，例如：

- “超过” 与 “不低于”
- “普通决议” 与 “特别决议”
- “以较高者为准” 与 “以较低者为准”

因此只搜题干会漏掉核心反证或支持证据。

### 6.3 Qwen 证据筛选模块

#### 功能目标

- 从规则召回得到的候选片段中筛出真正相关证据
- 标记噪声段落
- 指出仍然证据不足的选项

#### 模块定位

该模块在 MVP 中承担“合规 reranker”的角色，但它不直接产出答案，只输出：

- 哪些 chunk 与哪个选项相关
- 哪些 chunk 是噪声
- 哪些选项仍缺证据

#### 输入输出

输入：

- 题目
- 选项
- 候选 chunk 列表

输出：

- 相关证据列表
- 噪声列表
- `insufficient_options`

### 6.4 Qwen 逐选项推理模块

#### 功能目标

- 对 A/B/C/D 逐项给出判断
- 统一输出 `support / refute / insufficient`
- 对涉及数值的问题展示比较或计算依据

#### 推理原则

- 不直接输出最终字母
- 先做选项级判定
- 最终答案由后处理模块按题型规则合成

#### 三类题型处理规则

`单选题`：

- 每个选项独立判断
- 选择唯一 `support` 项
- 若多个选项都支持，则进入冲突消解

`多选题`：

- 只有“证据明确支持”的选项才纳入最终答案
- `insufficient` 不纳入答案
- 输出前按字母排序并去重

`判断题`：

- 先解析题目中的选项映射
- 不写死 `A=true`
- 以题面选项定义为准

### 6.5 基础数值计算模块

#### 功能目标

- 把金融数值题从“纯生成”转为“抽取 + 计算 + 校验”
- 提升金额、比例、期限类问题的稳定性

#### 推荐工具函数

- 比例换算
- 金额核算
- 利息测算
- 期限计算
- 简单现金流求和
- 大小关系比较
- 区间条件判断

#### 标准流程

1. 从证据中抽取数字和条件
2. 判断计算或比较类型
3. 调用工具函数
4. 将结果回写到逐选项判断中

### 6.6 结构化记忆压缩模块

#### 功能目标

- 在多轮推理中保存核心证据和事实
- 避免同题反复重读相同文档片段
- 用“证据账本”替代泛化摘要

#### 建议分三层保存

`原始证据记忆`：

- `doc_id`
- `page_no`
- `section_path`
- `quoted_text`
- `option_ref`

`结构化事实记忆`：

- 主体
- 年份
- 指标
- 数值
- 条件
- 关系

`推理状态记忆`：

- `supported`
- `refuted`
- `insufficient`
- 缺失字段
- 是否需要补检索

#### 推荐结构

```json
{
  "qid": "",
  "question_focus": [],
  "facts": [],
  "option_status": {},
  "missing_fields": [],
  "followup_needed": false
}
```

### 6.7 答案格式化与提交模块

#### 功能目标

- 将逐选项判断转换为最终提交格式
- 避免格式错误导致失分
- 同步输出证据文件和 Token 统计

#### 输出物

- `answer.csv`
- `evidence.json`
- `logs/`

#### `answer.csv` 约束

- 首行为 `summary`
- 记录总 `prompt_tokens`、`completion_tokens`、`total_tokens`
- 后续行为每道题的 `qid` 和 `answer`

#### `evidence.json` 建议字段

```json
{
  "qid": "",
  "domain": "",
  "answer": "",
  "evidence_retrieval": [
    {
      "doc_id": "",
      "page_no": 0,
      "clause_no": "",
      "quoted_clause": "",
      "supports_option": [],
      "reasoning": ""
    }
  ],
  "token_usage": {
    "prompt_tokens": 0,
    "completion_tokens": 0,
    "total_tokens": 0
  }
}
```

## 7. 技术选型建议

| 模块 | 推荐技术 |
|---|---|
| PDF解析 | PyMuPDF |
| OCR | PaddleOCR |
| 表格/结构恢复 | pdfplumber 或等效工具 |
| 文本清洗 | Python Regex / 规则引擎 |
| 文档级召回 | BM25 / Whoosh / Elasticsearch |
| 片段级召回 | BM25 + 条款号/实体/数字匹配 |
| 正式答题模型 | Qwen 系列 API |
| 缓存 | 本地缓存或 Redis |
| 数据存储 | SQLite / PostgreSQL |
| 服务框架 | FastAPI |

### 7.1 技术选型说明

- MVP 正式答题阶段不将向量模型作为主链路组件写入方案。
- 若后续做实验性检索增强，可放入全链路版的“研究型扩展”中，而非 MVP 正式链路。
- Qwen 调用应统一收口到单一客户端，便于统计 Token 和审计调用边界。

## 8. 数据设计

### 8.1 核心数据对象

- `Document`：原始文档信息
- `StructuredChunk`：结构化文档片段
- `QueryTask`：题目任务
- `RetrievedEvidence`：候选证据与精选证据
- `ReasoningResult`：逐选项判断结果
- `MemoryItem`：结构化记忆项
- `SubmissionRecord`：提交结果和 Token 记录

### 8.2 关键表建议

- 文档表
- 结构化 chunk 表
- 倒排索引映射表
- 查询日志表
- 证据链表
- 记忆表
- Token 统计表

## 9. 部署方案

### 9.1 推荐部署形态

建议 MVP 版本采用单机或单服务器部署：

- 前端：Streamlit 或轻量 Web 页面
- 后端：FastAPI
- 检索：Whoosh / Elasticsearch / 本地 BM25
- 存储：SQLite
- 模型：Qwen API

### 9.2 工程目录建议

```text
submission/
├── answer.csv
├── evidence.json
├── processed_data/
├── agent/
├── script/
├── logs/
├── requirements.txt
└── README.md
```

## 10. 实施路线

### 10.1 第一阶段：合规基线

- 完成 PDF/OCR 文档接入
- 完成结构化切片
- 完成 A 榜模式下的文档内词法检索
- 完成单轮 Qwen 推理
- 完成 `answer.csv` 输出

### 10.2 第二阶段：准确率提升

- 增加条款级和表格级切片
- 增加选项级召回
- 增加 Qwen 证据筛选
- 增加基础数值计算工具

### 10.3 第三阶段：Token 优化与 B 榜准备

- 增加文档级召回
- 增加结构化记忆压缩
- 增加补检索机制
- 增加 `evidence.json` 和日志输出

## 11. 验收指标建议

- A 榜样例可稳定跑通，提交格式零错误
- 证据定位召回率 `Recall@5 >= 95%`
- 证据定位准确率 `Precision@5 >= 92%`
- 基础数值计算准确率 `>= 95%`
- Token 消耗相较全文输入方案显著下降
- 关键结论的证据可追溯率 `= 100%`

## 12. 优势与局限

### 12.1 优势

- 更贴合赛题真实约束，合规边界清晰
- 以词法检索打底，工程实现稳定
- 把 Qwen 调用集中在高价值环节，利于控 Token
- 输出物与代码结构直接面向竞赛提交要求

### 12.2 局限

- 单纯词法召回对语义改写问题仍有上限
- 对复杂跨文档多跳推理支持有限
- 结构化记忆仍偏任务内复用，长期记忆能力较弱
- 对复杂表格和低质量扫描件的效果高度依赖预处理质量

## 13. 结论

MVP 版本以“先合规、先跑通、先能提交”为目标，强调 `离线重预处理、在线轻上下文、词法召回打底、Qwen 精筛与逐选项推理收口`。相较于泛化的长文档问答方案，这一版本更贴近赛题真实评测方式，也更适合作为竞赛首版系统的技术基线。后续若需要扩展更强的结构化比较、分层记忆和全链路治理能力，可在此基础上迭代到全链路版本。
