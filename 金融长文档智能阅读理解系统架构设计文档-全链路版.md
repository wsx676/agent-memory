# 金融长文档智能阅读理解系统架构设计文档（全链路版）

## 1. 文档说明

### 1.1 文档目的

本文档面向竞赛增强版与最终提交阶段，给出“金融长文档智能阅读理解系统”的全链路系统架构设计方案。全链路版本建立在 MVP 之上，不再只追求“能跑通”，而是围绕赛题真实约束，补强 `文档级召回、领域化规则、结构化比较、证据账本、答案校验、Token 治理与代码审计交付` 等关键能力，形成一套更适合冲榜和代码复审的增强型架构。

### 1.2 赛题约束下的建设目标

结合工作区中的两份赛题分析文档，全链路版本聚焦以下目标：

- 在合规前提下提升 A 榜准确率，并为 B 榜无 `doc_ids` 模式建立稳定链路
- 实现 `离线重预处理 + 在线轻上下文` 的完整 Agent 流水线
- 构建领域化、题型化、选项级的检索与推理体系
- 将多轮推理沉淀为可审计、可复用的结构化证据账本
- 形成完整的 `answer.csv`、`evidence.json`、日志、脚本和目录规范

### 1.3 适用范围

本版本适用于以下场景：

- 竞赛增强版方案设计
- A 榜中后期准确率冲刺
- B 榜无文档提示场景的盲检索与推理
- 金融产品、监管法规、金融合同、财报、研报等多领域联合分析

## 2. 设计原则

- `合规优先`：正式答题阶段的证据筛选、推理判断、必要压缩统一由 Qwen 承担。
- `准确率优先`：评分公式中准确率是主导项，Token 优化建立在不掉点前提下。
- `结构化优先`：检索、证据、事实、比较结果、答案均尽量转成结构化对象。
- `分层治理`：预处理、召回、推理、记忆、校验、日志、提交分别治理。
- `可审计交付`：目录结构、脚本入口、日志字段、证据格式从一开始按代码审核要求组织。

## 3. 全链路目标能力

全链路版本建议实现以下能力：

- 多源金融文档的结构恢复、表格恢复与条款切片
- A 榜和 B 榜双模式下的文档级与片段级检索
- 题干级 + 选项级双通道证据召回
- Qwen 证据筛选与逐选项判断
- 结构化字段抽取与跨文档深度比较
- 数值计算、条件推理、公式匹配与结果校验
- 原始证据记忆、事实记忆、推理状态记忆三层压缩
- Token 优化、运行日志、证据回溯与提交打包

## 4. 系统总体架构

### 4.1 分层架构

全链路版本采用六层架构：

1. `入口与编排层`：负责题目接入、任务解析、题型识别、A/B 榜路由。
2. `离线预处理层`：负责 OCR、版面分析、表格恢复、结构化切片和索引构建。
3. `规则召回层`：负责文档级候选召回、片段级候选召回和规则打分。
4. `Qwen 推理层`：负责证据精筛、逐选项判断、必要时补检索和压缩。
5. `结果与治理层`：负责答案格式化、证据链、校验、记忆账本和 Token 管理。
6. `交付与审计层`：负责 `answer.csv`、`evidence.json`、日志、脚本和打包输出。

### 4.2 总体流程

```text
题目输入 / 文档输入
        |
        v
入口与编排层（题型识别、领域识别、A/B榜路由）
        |
        v
离线预处理层（OCR、结构恢复、表格抽取、条款切片、索引）
        |
        v
规则召回层（doc-level召回 -> chunk-level召回 -> 规则打分）
        |
        v
Qwen 推理层（证据精筛 -> 逐选项判断 -> 必要补检索 -> 压缩）
        |
        v
结果与治理层（答案合成、证据链、记忆账本、Token统计、校验）
        |
        v
交付与审计层（answer.csv / evidence.json / logs / scripts）
```

### 4.3 全链路逻辑架构图

```text
用户入口 / 批处理脚本 / API
        |
        v
任务编排器
  ├─ 题型识别
  ├─ 领域识别
  ├─ A/B榜模式路由
  └─ 检索与推理计划生成
        |
        v
离线资产层
  ├─ 原始PDF
  ├─ OCR结果
  ├─ 结构化chunk/table/clause
  └─ 规则索引
        |
        v
在线答题层
  ├─ doc-level召回
  ├─ chunk-level召回
  ├─ Qwen证据筛选
  ├─ Qwen逐选项推理
  ├─ 工具计算
  └─ 缺口补检索
        |
        v
治理输出层
  ├─ 证据账本
  ├─ 答案格式化
  ├─ Token统计
  ├─ 运行日志
  └─ 提交打包
```

## 5. 赛题适配设计

### 5.1 合规边界

全链路版本必须严格遵守以下边界：

- 允许在离线预处理阶段使用 OCR、版面恢复、表格提取、PDF 结构化等非 Qwen 工具。
- 不在正式答题链路中使用非 Qwen 模型生成的向量、rerank 结果和语义判断。
- 正式答题阶段的检索主链路仍以词法/规则为主，Qwen 负责精筛和推理。
- 结构化知识、缓存和记忆账本仅允许存放来自规则抽取或 Qwen 当前任务确认后的结果。

### 5.2 A 榜与 B 榜分流

全链路版必须内建两套运行模式：

`A 榜模式`

- 题目提供 `doc_ids`
- 直接在指定文档内完成 chunk 召回和推理
- 用于高频调试、Prompt 调优和选项级策略验证

`B 榜模式`

- 题目不提供 `doc_ids`
- 先做候选文档召回，再做片段级召回与推理
- 更依赖文档级打分、领域分流和证据收缩能力

### 5.3 评分导向

全链路版的优化顺序明确为：

- 第一优先：准确率
- 第二优先：提交合法性与稳定性
- 第三优先：Token 治理

因此增强链路的价值不在“多堆模块”，而在于通过更精细的分流与记忆压缩，在不损失准确率的情况下减少无效调用。

## 6. 核心模块设计

### 6.1 任务编排与规则 Planner 模块

#### 功能目标

- 解析题目中的领域、题型、实体、年份、金额、条款号等关键信息
- 判断走 A 榜还是 B 榜链路
- 决定是否需要比较、计算、补检索或记忆压缩

#### 输出内容

- `domain`
- `answer_format`
- `known_doc_ids`
- `entities`
- `numbers`
- `clause_refs`
- `retrieval_plan`
- `reasoning_plan`

#### 模块定位

该模块不调用模型，完全采用规则与模板解析，目的是最大限度压缩后续模型调用目标。

### 6.2 高级文档解析与结构恢复模块

#### 功能目标

- 恢复复杂 PDF 中的章节、条款、表格和阅读顺序
- 将原始文档转成可检索、可定位、可回指的结构化资产
- 为财报、合同、法规等不同类型文档建立稳定切片基础

#### 增强能力

- 页眉页脚识别
- 重复段与目录清洗
- 跨页段落合并
- 条款编号抽取
- 表格区域与表头恢复
- 多级章节路径恢复

#### 标准 chunk 字段建议

```json
{
  "doc_id": "",
  "title": "",
  "domain": "",
  "page_no": 1,
  "section_path": "",
  "chunk_id": "",
  "chunk_text": "",
  "chunk_type": "prose/table/clause/header/footer/appendix",
  "clause_no": "",
  "table_title": "",
  "entities": [],
  "numbers": [],
  "source_span": ""
}
```

#### 切片策略

- 监管法规：按 `条 / 款 / 项` 做强结构切片
- 财报：按 `章节 + 表格独立块` 切片
- 保险条款：按 `责任 / 免责 / 给付 / 退保 / 现金价值` 切片
- 金融合同：按 `发行条款 / 票息 / 评级 / 偿付顺序 / 时间安排` 切片
- 研报：按 `观点块 / 风险提示 / 盈利预测` 切片

### 6.3 文档级召回模块

#### 功能目标

- 在 B 榜模式下先从候选文档集中找出最相关的 3~8 篇文档
- 避免将大量无关文档带入片段检索和 Qwen 推理

#### 召回策略

- BM25
- 标题词重合
- 实体重合
- 条款号命中
- 数字字段命中
- 领域过滤

#### 打分思路

可用多特征加权方式对候选文档排序：

- `BM25`
- `TitleSim`
- `EntityOverlap`
- `ClauseHit`
- `NumHit`

监管法规任务提高 `ClauseHit` 权重，财报任务提高 `NumHit` 和表格先验，研报任务提高标题词和实体重合权重。

### 6.4 片段级召回模块

#### 功能目标

- 在候选文档中召回最可能支持或反驳选项的证据片段
- 为 Qwen 证据筛选层提供高质量候选输入

#### 核心策略

- 不只检索题干
- 对题干和每个选项分别检索
- 召回题干相关片段和 A/B/C/D 各自相关片段
- 对结果去重、合并相邻 chunk、按片段类型加权

#### 片段类型先验

- 法规题优先 `clause`
- 财报题优先 `table`
- 保险和合同题优先规则型 `clause + prose`
- 研报题优先 `观点块`

### 6.5 Qwen 证据筛选模块

#### 功能目标

- 对片段级召回结果进行合规精筛
- 识别每段证据与哪个选项相关
- 剔除噪声段并标记证据缺口

#### 模块输出

- `relevant`
- `noise`
- `insufficient_options`
- `need_followup_retrieval`

#### 设计原则

- 只筛证据，不下最终结论
- 强制 JSON 输出
- 无证据不臆断

### 6.6 Qwen 逐选项推理模块

#### 功能目标

- 基于精选证据对每个选项独立判定
- 输出 `support / refute / insufficient`
- 对数值、比例、期限类问题显式给出比较过程

#### 题型处理规则

`单选题`

- 逐项判断
- 对多个支持项做冲突消解
- 输出唯一答案

`多选题`

- 只将“证据明确支持”的选项纳入答案
- `insufficient` 一律不纳入
- 输出前排序与去重

`判断题`

- 先解析题面中的选项映射
- 不能写死 `A=true/B=false`

### 6.7 结构化比较分析模块

#### 功能目标

- 将多份金融文档中的关键信息抽取为统一字段
- 完成字段对齐、差异分类和影响说明
- 输出结构化差异报告，并为答案附着证据

#### 处理流程

1. 定义文档类型字段 schema
2. 从证据块中抽取字段
3. 对多文档字段做归一化与对齐
4. 输出差异项及影响说明

#### 差异类型

- 数值差异
- 期限差异
- 主体差异
- 责任范围差异
- 条件触发差异
- 表述差异

#### 模块定位

该模块是 MVP 的增强能力，不用于替代问答链路，而用于补强多文档比较题和复杂报告型题目。

### 6.8 数值计算与条件推理模块

#### 功能目标

- 从证据中抽取参数、条件与公式触发规则
- 使用工具执行数值计算
- 将结果回写到逐选项判断或比较报告

#### 建议子模块

- 条件抽取器
- 参数标准化器
- 公式匹配器
- 工具执行器
- 结果校验器

#### 典型场景

- 资产负债率阈值判断
- 保险给付公式带入
- 年度指标同比比较
- 票息、规模、期限等合同条件比对

### 6.9 三层证据账本与记忆压缩模块

#### 功能目标

- 将多轮推理结果压缩为可复用、可审计、可控 Token 的结构化账本
- 支撑补检索、跨轮对比与 B 榜复杂题复核

#### 三层记忆结构

`原始证据记忆`

- `doc_id`
- `page_no`
- `section_path`
- `quoted_text`
- `option_ref`

`结构化事实记忆`

- 主体
- 年份
- 指标
- 数值
- 条件
- 关系

`推理状态记忆`

- `evidence_found`
- `supported`
- `refuted`
- `insufficient`
- `missing_fields`
- `need_followup_retrieval`

#### 推荐结构

```json
{
  "qid": "",
  "question_focus": [],
  "facts": [],
  "option_status": {},
  "missing_fields": [],
  "followup_needed": false
}
```

#### 压缩原则

- 压缩的是“可复用事实”和“缺口状态”，不是泛化摘要
- 只在有必要补检索或跨轮复用时调用压缩
- 同题内优先补缺口，不整题重跑

### 6.10 答案校验与抗干扰模块

#### 功能目标

- 检查答案是否真的由证据支持
- 对细微表述差异执行二次校验
- 防止误把“高相似度噪声段”当作支持证据

#### 校验维度

- 证据存在性
- 条件一致性
- 数值一致性
- 选项映射一致性
- 文档来源一致性

### 6.11 提交格式与审计交付模块

#### 功能目标

- 生成竞赛可直接提交的标准产物
- 为 B 榜前列代码审计提前组织目录和脚本

#### 输出物

- `answer.csv`
- `evidence.json`
- `processed_data/`
- `agent/`
- `script/`
- `logs/`
- `requirements.txt`
- `README.md`

#### `evidence.json` 建议字段

```json
{
  "qid": "",
  "domain": "",
  "answer": "",
  "evidence_retrieval": [
    {
      "doc_id": "",
      "page_no": 0,
      "clause_no": "",
      "quoted_clause": "",
      "supports_option": [],
      "reasoning": ""
    }
  ],
  "token_usage": {
    "prompt_tokens": 0,
    "completion_tokens": 0,
    "total_tokens": 0
  }
}
```

## 7. 核心任务流程

### 7.1 A 榜答题流程

```text
读取题目与已知 doc_ids
  -> 规则 Planner 解析题型和领域
  -> 指定文档内 chunk 召回
  -> Qwen 证据筛选
  -> Qwen 逐选项判断
  -> 工具计算与结果回写
  -> 答案格式化与证据输出
```

### 7.2 B 榜答题流程

```text
读取题目
  -> 规则 Planner 解析实体和领域
  -> doc-level 候选文档召回
  -> chunk-level 题干/选项级召回
  -> Qwen 证据筛选
  -> Qwen 逐选项判断
  -> 如有证据缺口则补检索
  -> 压缩账本并输出结果
```

### 7.3 多文档比较流程

```text
输入多份文档与比较任务
  -> 文档结构恢复
  -> 字段 schema 定义
  -> 证据块抽取
  -> 字段对齐
  -> 差异分类
  -> 挂载证据与影响分析
  -> 输出比较报告
```

### 7.4 数值推理流程

```text
输入计算型问题
  -> 证据召回
  -> 条件与参数抽取
  -> 工具计算
  -> Qwen 结果解释
  -> 答案校验与输出
```

## 8. 领域与题型增强策略

### 8.1 领域增强

`保险条款`

- 强化给付公式、等待期、现金价值、免责条件抽取
- 重点识别 `min/max`、`以较大者为准` 等规则表达

`监管法规`

- 强化条款级索引
- 重点识别 `必须`、`普通决议`、`特别决议`、`生效时间`、`例外情形`

`金融合同`

- 强化发行规模、票息、评级、偿付顺序、时点安排的跨段联合检索

`财务报表`

- 强化表格恢复
- 重点抽取 `年度-指标名-数值-单位-同比`

`行业研报`

- 强化观点块、盈利预测、风险提示与证据对应关系

### 8.2 题型增强

- 单选题：互斥消解
- 多选题：保守纳入，严控误选
- 判断题：先做选项映射，再做真假判断

## 9. 技术选型建议

| 模块 | 推荐技术 |
|---|---|
| PDF解析 | PyMuPDF / pdfplumber |
| OCR | PaddleOCR |
| 结构恢复 | 规则引擎 + PDF结构化工具 |
| 文档级召回 | BM25 / Whoosh / Elasticsearch |
| 片段级召回 | BM25 + 条款号/实体/数字匹配 |
| 正式答题模型 | Qwen 系列 API |
| 数据库存储 | PostgreSQL |
| 缓存 | Redis |
| 任务编排 | FastAPI + Celery / 自定义编排器 |
| 监控 | Prometheus + Grafana |

### 9.1 技术选型说明

- 正式答题链路中，不将第三方向量模型、第三方 reranker 作为主链路组件。
- 如果后续要尝试研究型增强，应与正式链路隔离，不进入提交版。
- Qwen 调用统一收口到单一客户端，便于计 Token、记日志、做审计。

## 10. 数据设计

### 10.1 核心数据对象

- `Document`
- `StructuredChunk`
- `QueryTask`
- `RetrievedEvidence`
- `ReasoningResult`
- `CompareResult`
- `CalcResult`
- `MemoryLedger`
- `SubmissionRecord`

### 10.2 存储设计建议

- 原始文档库存储 PDF、OCR 结果和中间结构化产物
- 结构化文档库存储 chunk、table、clause 三类切片
- 检索库存储倒排索引、条款索引、实体索引和数值索引
- 记忆库存储事实账本与推理状态
- 日志库存储题目日志、Qwen 响应、Token 消耗和错误信息

## 11. 非功能设计

### 11.1 性能设计

- 单次问答平均响应时间控制在 `8~12 秒`
- 文档级召回控制在 `2 秒` 内
- 支持中小规模文档库稳定运行

### 11.2 可扩展性设计

- 规则召回层和 Qwen 推理层解耦
- 文档解析与答题链路解耦
- 比较分析、计算、压缩可作为独立服务扩展

### 11.3 可观测性设计

建议记录以下日志：

- 文档预处理日志
- 召回日志
- 证据筛选日志
- 逐选项判断日志
- Token 消耗日志
- 格式校验日志
- 错误追踪日志

## 12. 部署方案

### 12.1 推荐部署形态

建议全链路版本采用服务化部署：

- 前端：Vue / React 或答辩展示页
- 后端：FastAPI
- 异步任务：Celery 或任务队列
- 检索：Elasticsearch / Whoosh + 本地索引
- 缓存：Redis
- 数据库：PostgreSQL
- 监控：Prometheus + Grafana

### 12.2 工程目录建议

```text
submission/
├── answer.csv
├── evidence.json
├── processed_data/
│   ├── docs_structured/
│   ├── chunks/
│   ├── tables/
│   └── lexical_index/
├── agent/
│   ├── planner.py
│   ├── retriever.py
│   ├── evidence_selector.py
│   ├── reasoner.py
│   ├── memory_manager.py
│   ├── answer_formatter.py
│   └── token_tracker.py
├── script/
│   ├── preprocess.py
│   ├── build_index.py
│   ├── run_a.py
│   ├── run_b.py
│   └── package_submission.py
├── logs/
├── requirements.txt
└── README.md
```

## 13. 实施路线

### 13.1 第一阶段：增强检索

- 完成文档级召回
- 完成条款级和表格级切片
- 完成题干级与选项级双召回

### 13.2 第二阶段：增强推理

- 完成 Qwen 证据筛选
- 完成逐选项判断
- 完成数值工具链和领域 Prompt

### 13.3 第三阶段：增强治理

- 完成证据账本
- 完成缺口补检索
- 完成答案校验和格式化

### 13.4 第四阶段：增强交付

- 完成 `evidence.json` 固定格式
- 完成全量日志
- 完成一键运行与提交打包
- 完成代码审计准备

## 14. 验收指标建议

- A 榜样例和验证集稳定跑通
- B 榜链路支持无 `doc_ids` 候选文档召回
- 证据定位召回率 `Recall@5 >= 95%`
- 证据定位准确率 `Precision@5 >= 92%`
- 跨文档比较准确率 `>= 90%`
- 数值计算准确率 `>= 95%`
- 抗干扰测试集准确率 `>= 93%`
- 关键结论的证据可追溯率 `= 100%`

## 15. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| OCR 与表格恢复质量不足 | 直接拉低财报和合同题准确率 | 提高预处理投入，优先优化结构恢复 |
| B 榜文档级召回失准 | 后续推理全部失效 | 做领域分流和多特征打分，保留 3~8 篇候选 |
| 多选题误选 | 整题直接判错 | 采用保守策略，仅纳入明确支持项 |
| 长上下文重复喂入 | Token 快速膨胀 | 启用证据账本与缺口补检索机制 |
| 非法格式提交 | 直接失分 | 用独立格式化与校验模块统一出包 |

## 16. 结论

全链路版本不是对 MVP 的简单放大，而是在赛题硬约束下构建的增强型可提交架构。它以 `离线重预处理、在线轻上下文、规则召回打底、Qwen 精筛与逐选项推理收口、证据账本控 Token、格式治理保提交` 为核心设计思想，更适合作为竞赛增强版方案、B 榜准备方案以及后续代码审计交付方案。
